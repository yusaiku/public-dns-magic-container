# Minimale, stabile Knot Resolver Konfiguration

server:
  listen:
    - interface: 127.0.0.1
      port: 53

# DNSSEC aktiv
dnssec: true

# Cache
cache:
  size: 300M

# Logging (hilft beim Debuggen)
logging:
  level: info
